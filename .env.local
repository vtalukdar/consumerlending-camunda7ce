# Camunda 7 Docker image selection based on platform
# Windows: uses amd64 compatible image
# macOS/Linux: uses compatible image (auto-detects ARM/x86)
CAMUNDA_IMAGE=camunda/camunda-bpm-platform:run-latest